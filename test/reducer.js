// import {reducer} from '../src/reducer';


describe('reducer', () => {
  it('should ignore unrelated action types');
  it('should raise if action location starts with missing store entry point');
  it('should put new data into empty location');
  it('should merge new data into not empty location');
});
